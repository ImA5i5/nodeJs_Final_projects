<!-- views/pages/client/dashboard.ejs -->


<h1 class="text-3xl font-bold text-gray-800 mb-6">Welcome, Client 👋</h1>

<div class="grid grid-cols-1 md:grid-cols-3 gap-6">
  <div class="bg-green-500 text-white rounded-lg p-6 shadow-md">
    <h3 class="text-lg font-semibold mb-2">📁 Projects Posted</h3>
    <p class="text-3xl font-bold"><%= projects?.length || 0 %></p>
  </div>

  <div class="bg-blue-500 text-white rounded-lg p-6 shadow-md">
    <h3 class="text-lg font-semibold mb-2">💸 Total Spent</h3>
    <p class="text-3xl font-bold">₹<%= totalSpent || 0 %></p>
  </div>

  <div class="bg-yellow-500 text-white rounded-lg p-6 shadow-md">
    <h3 class="text-lg font-semibold mb-2">⭐ Average Rating Given</h3>
    <p class="text-3xl font-bold"><%= avgRating || 0 %></p>
  </div>
</div>

<div class="mt-10 bg-white rounded-lg shadow p-6">
  <h2 class="text-xl font-bold mb-4">Recent Projects</h2>
  <ul class="divide-y">
    <% projects?.slice(0, 5).forEach(p => { %>
      <li class="py-3">
        <div class="flex justify-between">
          <span><strong><%= p.title %></strong></span>
          <span class="text-sm text-gray-500 capitalize"><%= p.status %></span>
        </div>
        <p class="text-gray-600 text-sm"><%= p.description.substring(0, 80) %>...</p>
      </li>
    <% }) %>
  </ul>
</div>
