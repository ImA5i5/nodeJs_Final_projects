<!-- views/pages/freelancer/dashboard.ejs -->


<h1 class="text-3xl font-bold text-gray-800 mb-6">Welcome, Freelancer Dashboard 👋</h1>

<div class="grid grid-cols-1 md:grid-cols-3 gap-6">
  <div class="bg-purple-500 text-white rounded-lg p-6 shadow-md">
    <h3 class="text-lg font-semibold mb-2">💼 Total Bids</h3>
    <p class="text-3xl font-bold"><%= bids?.length || 0 %></p>
  </div>

  <div class="bg-green-500 text-white rounded-lg p-6 shadow-md">
    <h3 class="text-lg font-semibold mb-2">💰 Total Earnings</h3>
    <p class="text-3xl font-bold">₹<%= earnings || 0 %></p>
  </div>

  <div class="bg-blue-500 text-white rounded-lg p-6 shadow-md">
  <h3 class="text-lg font-semibold mb-2">⭐ Average Rating</h3>
  <p class="text-3xl font-bold">
    <%= (user && user.profile && user.profile.rating) ? user.profile.rating : 0 %>
  </p>
</div>

<div class="mt-10 bg-white shadow rounded-lg p-6">
  <h2 class="text-xl font-bold mb-4">Recent Bids</h2>
  <table class="w-full text-left border">
    <thead class="bg-gray-100 text-sm uppercase text-gray-600">
      <tr>
        <th class="px-4 py-2 border">Project</th>
        <th class="px-4 py-2 border">Bid Amount</th>
        <th class="px-4 py-2 border">Status</th>
      </tr>
    </thead>
    <tbody>
      <% bids?.forEach(bid => { %>
        <tr class="border-b hover:bg-gray-50">
          <td class="px-4 py-2"><%= bid.project?.title || 'N/A' %></td>
          <td class="px-4 py-2">₹<%= bid.bidAmount %></td>
          <td class="px-4 py-2 capitalize"><%= bid.status %></td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</div>
